package org.opengis.cite.wps10.level1;

/*import java.io.IOException;
import javax.xml.parsers.DocumentBuilder;
import javax.xml.parsers.DocumentBuilderFactory;
import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Test;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;
import org.testng.ISuite;
import org.testng.ITestContext;
import org.w3c.dom.Document;
import org.xml.sax.SAXException;*/

/**
 * Verifies the behavior of the Capability1Tests test class. Test stubs replace
 * fixture constituents where appropriate.
 */
public class VerifyCapability1Tests {

	/*
	 * private static final String SUBJ = "testSubject"; private static
	 * DocumentBuilder docBuilder; private static ITestContext testContext; private
	 * static ISuite suite;
	 * 
	 * public VerifyCapability1Tests() { }
	 * 
	 * @BeforeClass public static void setUpClass() throws Exception { testContext =
	 * mock(ITestContext.class); suite = mock(ISuite.class);
	 * when(testContext.getSuite()).thenReturn(suite); DocumentBuilderFactory dbf =
	 * DocumentBuilderFactory.newInstance(); dbf.setNamespaceAware(true); docBuilder
	 * = dbf.newDocumentBuilder(); }
	 * 
	 * @AfterClass public static void tearDownClass() throws Exception { }
	 * 
	 * @Test public void testTrim() { // Capability1Tests iut = new
	 * Capability1Tests(); // iut.trim(); }
	 * 
	 * @Test(expected = NullPointerException.class) public void
	 * supplyNullTestSubject() throws SAXException, IOException { //
	 * Capability1Tests iut = new Capability1Tests(); // iut.NotEmpty(); }
	 */
    
}
